// Kinergy Platform - Prisma Database Schema
// Datasource: PostgreSQL

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Infrastructure Baseline (No domain entities defined yet as per architecture guidelines)

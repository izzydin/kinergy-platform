import { Slot, Slottable } from '@radix-ui/react-slot';

/**
 * Polymorphic Composition Primitives
 *
 * Export Radix UI `Slot` and `Slottable` primitives to power the `asChild` composition pattern
 * across all interactive components without component-specific wrapper props.
 */
export { Slot, Slottable };
export type { SlotProps } from '@radix-ui/react-slot';
